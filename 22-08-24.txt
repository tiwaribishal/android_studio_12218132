  //check all thye fuctions which are available in arrays in kotlin should be implemented in program 
  
val letter: Char
letter = 'A'
println(letter)
println("\n")  // Corrected to use println and fixed escape sequence
print('/$')  // This is valid, no change needed


fun main()
{
    val numbers=arrayOf(1, 2, 3, 4, 5)
    print(numbers[0])
    print(numbers[1])
    print(numbers[2])
    print(numbers[3])
    println(numbers[4])
  	println("Numbers array size = "+numbers.size)
  // val words:Array<String>=arrayOf("kotlin","Java","Python")//
   //lamda fuction
  // val squares=Array(5){i -> i*i}
}

fun main()
{
 //create 2 d array
    val matrix: Array<Array<Int>>=
}
